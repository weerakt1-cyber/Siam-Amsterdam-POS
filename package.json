{
  "name": "baze",
  "private": true,
  "version": "0.0.0",
  "description": "Baze platform monorepo — apps/pos (+ future apps/admin, apps/affiliate) over one Supabase DB.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/pos",
    "build": "npm run build --workspace apps/pos",
    "lint": "npm run lint --workspace apps/pos"
  }
}
